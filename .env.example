# signal = recommendations only; paper = recommendations + virtual portfolio
MODE=signal

# intraday = 15m EMA 9/21; swing = 4h EMA 12/26
STRATEGY=intraday

# From https://portal.jup.ag/ — recommended for stable quotes
JUPITER_API_KEY=

# Comma-separated pairs (v1 supports SOL/USDC)
WATCHLIST=SOL/USDC

# How often to poll candles + quote (ms)
POLL_INTERVAL_MS=60000

# Starting virtual USDC balance for paper mode
PAPER_CASH_USDC=1000

# Optional: explicit GeckoTerminal Solana pool address for OHLCV.
# If empty, a default SOL/USDC pool is used.
# GECKO_POOL_ADDRESS=
